# MJR Blaster

A lightweight, client-only typing game with zero runtime dependencies.

## Run locally

Option A: Open `public/index.html` directly in a browser.

Option B: Run a tiny local server (Node, no deps):

```bash
npm run dev
```

Then visit `http://localhost:5173`.
